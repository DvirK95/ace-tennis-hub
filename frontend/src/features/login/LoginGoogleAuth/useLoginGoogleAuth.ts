function useLoginGoogleAuth() {
  function handleGoogleLogin() {
    // Preparation only: implement Google OAuth later.
    // e.g. redirect to backend endpoint or use Google Identity Services.
  }

  return {
    handleGoogleLogin,
  };
}

export default useLoginGoogleAuth;
